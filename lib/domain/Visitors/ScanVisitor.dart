import 'package:flutter/material.dart';

class ScanVisitor extends StatelessWidget {
  const ScanVisitor ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
