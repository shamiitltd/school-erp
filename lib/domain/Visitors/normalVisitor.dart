import 'package:flutter/material.dart';

class NormalVisitor extends StatelessWidget {
  const NormalVisitor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
